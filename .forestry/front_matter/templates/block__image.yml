---
label: block__image
hide_body: true
fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - type: select
    name: background
    config:
      source:
        type: simple
      options:
        - transparent
        - dark-blue
        - light-blue
        - light-pink
        - blue
        - pink
        - white
